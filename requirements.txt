# ---- Core (required) -------------------------------------------------
discord.py>=2.7.1
claude-agent-sdk>=0.1.81
python-dotenv>=1.2.2

# ---- Optional features (install only if you want them) ---------------
# /schedule command (cron expression scheduling):
croniter>=6.2.2
# Voice-message transcription (local Whisper speech-to-text):
faster-whisper>=1.2.1
#   NVIDIA GPU acceleration (optional; runs on CPU without these). Install them
#   INTO THE SAME venv, NOT the system Python -- otherwise the bot can't load
#   cublas64_12.dll and voice transcription fails:
#       pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
# Drive-mode voice REPLY (local F5-TTS text-to-speech). Only used when you turn on
# /drive. The F5-TTS model is under CC-BY-NC 4.0, which FORBIDS commercial use. Much
# simpler to install than the old XTTS path -- no FFmpeg/torchcodec DLLs, no license
# env var. The reference voice reuses f5-tts's own bundled sample, so there's nothing
# extra to download or place:
#       f5-tts>=1.1.20
#   For NVIDIA GPU (recommended; on CPU it's minutes per reply) install a CUDA 12.x
#   build of torch INTO THE SAME venv, e.g.:
#       pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#   Full, tested details in the README section "Drive mode (voice reply)".
# PowerPoint -> PDF conversion (Windows only, requires PowerPoint installed):
pywin32>=312
# Semantic /search (find sessions by meaning, not just literal keywords). Without it,
# /search transparently falls back to literal keyword matching. CPU-only, no PyTorch
# (onnxruntime backend); first search downloads the multilingual-e5-large retrieval
# model once (~2.2GB):
fastembed>=0.3.0
